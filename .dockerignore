node_modules
dist

# Version control
.git
.gitignore

# Secrets — never bake into the image
.env
.env.*
!.env.example

# Test code — not needed in any build stage
tests

# Editor / tooling
.vscode
.zed
*.md

# CI / GitHub Actions
.github
